using UnityEngine;
using System.Collections;

public class ZrobymMarkersEvent : MonoBehaviour {

    //Events
    //The ARTrackedObject generates the following events using Unity's SendMessage command.
    //To handle these events, implement the matching event handler in a script, and attach it to the ARCamera. 
    //void OnMarkerLost(ARMarker marker)
    //void OnMarkerFound(ARMarker marker)
    //http://augmentmy.world/marker-detection-unity3d
}
